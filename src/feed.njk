---
permalink: feed.xml
---

<rss version="2.0">
  <title>moosyus awesome blog</title>
  <description>moosyu is online</description>
  <link>https://moosyu.github.io</link>
  <channel>
  {%- for post in collections.blog | reverse %}
  {%- set absolutePostUrl %}{{ post.url | htmlBaseUrl(metadata.base) }}{% endset %}
    <item>
        <title>{{ post.data.title }}</title>
        <description>{{ post.data.description }}</description>
        <content type="html">{{ post.content | renderTransforms(post.data.page, metadata.base) }}</content>
        <link> {{ absolutePostUrl }} </link>
        <guid>{{ absolutePostUrl }}</guid>
        <pubDate>{{ post.date }}</pubDate>
        {% raw %}
        <style>
        .post-box .post-title {
            background-image: url('https://moosyu.github.io/assets/thirtyvirus-5.avif');
            background-size: cover;
            background-position: center;
            color: white;
            padding: 1em;
        }
        .post-title, .post-date, post-dot, post-author, post-description {
            background-color: blue;
        }
        </style>
        {% endraw %}
    </item>
  {%- endfor %}
  </channel>
</rss>